[Definition]
# Deteguje HTTP 444 odpovede v NPM logoch
# Format: [timestamp] - - 444 - METHOD http ... [Client <HOST>]
failregex = ^\[.*?\]\s+(?:-\s+-\s+)?444\s+(?:-\s+)?(?:GET|POST|PUT|DELETE|PATCH|HEAD|OPTIONS)\s+.*?\[Client\s+<HOST>\]$
ignoreregex =
 
 
